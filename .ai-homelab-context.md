# AI CONTEXT — ROBYN'S HOMELAB

> Machine-oriented continuity file for future AI sessions.
>
> Read this file first, then inspect the current GitHub documentation before making infrastructure changes.
>
> This file describes the known deployed state as of **2026-09-09**.
>
> **Continuity note:** For changes after this date, read the latest dated AI handover in `docs/` (currently `docs/ai-handover-2026-09-21.md`) alongside this file before making infrastructure changes.

---

# 1. PROJECT

**Name:** Robyn's Homelab

**Primary domain:** `robynshomelab.dev`

**GitHub repository:** `RobynTW/robyns-homelab`

## Core goals

* Build a practical self-hosted homelab around Proxmox.
* Keep services appropriately separated between VMs and LXCs.
* Use Nginx as the reverse proxy for web services.
* Use Cloudflare for authoritative public DNS and DNS-01 certificate validation.
* Use NetBird for secure remote/LAN access without router port forwarding.
* Use the Dell OptiPlex 9020 as the secondary Proxmox/storage host.
* Provide network storage through host-mounted storage and NFS.
* Run Pterodactyl for game-server management.
* Run a dedicated media stack on CT103.
* Eventually deploy Homarr as a central dashboard.
* Maintain accurate GitHub documentation for future AI continuity.

## Working principles

* Preserve working infrastructure.
* Do not rebuild working services simply because documentation is incomplete.
* Make changes incrementally.
* Verify current state before destructive or broad changes.
* Clearly distinguish `DEPLOYED / VERIFIED`, `IN PROGRESS`, `PLANNED`, `RESERVED`, and `HISTORICAL`.
* Do not put credentials, API tokens, passwords, setup keys, private keys, cookies, or other secrets in GitHub or this file.
* When documentation conflicts, current deployed state takes precedence over historical plans.
* Do not introduce router port forwarding without an explicit architectural decision.
* Do not replace working storage, networking, or service architecture merely because another design may be theoretically preferable.

---

# 2. STATUS LEGEND

* `DEPLOYED / VERIFIED` — known to be installed and working.
* `IN PROGRESS` — partially configured or actively being completed.
* `PLANNED` — intended future work.
* `RESERVED` — VMID/resource reserved but not deployed.
* `NOT INSTALLED` — intended hardware/service is not currently installed.
* `HISTORICAL` — previous architecture or configuration; do not use as current state.

---

# 3. PHYSICAL HARDWARE

## pve-1

**Hostname:** `pve-1`

**IP:** `192.168.20.100`

**Hardware:** Dell OptiPlex 3060

**Role:** Primary Proxmox host.

Current workloads include:

* CT100 Pi-hole / Unbound / ddclient
* CT101 NetBird routing peer
* CT102 Jellyfin
* CT104 Beszel
* CT105 Uptime Kuma
* CT106 Nginx / Certbot

---

## pve-2

**Hostname:** `pve-2`

**IP:** `192.168.20.101`

**Hardware:** Dell OptiPlex 9020 SFF

**Role:** Secondary Proxmox host, Pterodactyl host, and physical storage host.

Hardware:

* Intel Core i7-4770
* 4 physical cores / 8 threads
* 3.40 GHz
* 16 GB RAM
* Samsung MZ7PC128HA SSD
* Approximately 119 GB SSD capacity
* 4 TB WD Blue `WD40EZRZ-00GXCB`

Proxmox:

* Proxmox VE 9.2.2
* Kernel previously observed as `7.0.2-6-pve`

Storage:

* SSD is the Proxmox system disk.
* 4 TB WD Blue is installed as additional host-mounted storage.
* HDD filesystem is ext4.
* HDD is mounted persistently at `/mnt/homelab-data`.
* NFS is served directly from pve-2.
* Pterodactyl game storage and general NAS/media storage are logically separated.

---

# 4. PROXMOX CLUSTER

**Cluster name:** `homelab`

Current state:

* `pve-1` and `pve-2` are members of the same Proxmox cluster.
* 2-node cluster.
* Quorum requires both nodes.
* HA is deliberately disabled.
* Do not describe pve-1 as standalone.
* Do not describe pve-2 as a future Proxmox host.

```text
homelab

├── pve-1  192.168.20.100
└── pve-2  192.168.20.101
```

Both nodes use:

```text
192.168.20.100 pve-1.home pve-1
192.168.20.101 pve-2.home pve-2
```

---

# 5. PROXMOX GUEST INVENTORY

| ID  | Type     | Host  | Name        | IP               | Role                            | Status              |
| --- | -------- | ----- | ----------- | ---------------- | ------------------------------- | ------------------- |
| 100 | CT       | pve-1 | pihole      | `192.168.20.99`  | Pi-hole + Unbound + ddclient    | DEPLOYED / VERIFIED |
| 101 | CT       | pve-1 | netbird     | `192.168.20.97`  | NetBird routing peer            | DEPLOYED / VERIFIED |
| 102 | CT       | pve-1 | jellyfin    | `192.168.20.98`  | Jellyfin                        | DEPLOYED / VERIFIED |
| 103 | CT       | pve-1 | mediastack  | `192.168.20.93`  | Media automation/download stack | DEPLOYED / VERIFIED |
| 104 | CT       | pve-1 | beszel      | `192.168.20.96`  | Monitoring                      | DEPLOYED / VERIFIED |
| 105 | CT       | pve-1 | uptime-kuma | `192.168.20.95`  | Monitoring                      | DEPLOYED / VERIFIED |
| 106 | CT       | pve-1 | nginx       | `192.168.20.94`  | Nginx reverse proxy + Certbot   | DEPLOYED / VERIFIED |
| 107 | reserved | pve-1 | homarr      | —                | Homarr dashboard                | RESERVED / PLANNED  |
| 108 | VM       | pve-2 | pterodactyl | `192.168.20.111` | Pterodactyl Panel + Wings       | DEPLOYED / VERIFIED |

---

# 6. NETWORK

## LAN

LAN subnet:

```text
192.168.20.0/24
```

Router:

```text
192.168.20.1
```

Current architecture is a flat LAN.

A managed switch and future VLAN segmentation may be introduced later.

## Important

There is currently **no router port forwarding for the homelab services**.

Do not add router port forwarding unless explicitly decided later.

---

# 7. SERVICE IP MAP

```text
192.168.20.93   CT103   Media stack
192.168.20.94   CT106   Nginx / Certbot
192.168.20.95   CT105   Uptime Kuma
192.168.20.96   CT104   Beszel
192.168.20.97   CT101   NetBird routing peer
192.168.20.98   CT102   Jellyfin
192.168.20.99   CT100   Pi-hole / Unbound / ddclient
192.168.20.100  pve-1
192.168.20.101  pve-2
192.168.20.111  VM108   Pterodactyl
```

## Critical address distinction

`192.168.20.99` is **Pi-hole/DNS**.

`192.168.20.94` is **Nginx/Certbot**.

`192.168.20.93` is **Media Stack**.

Do not merge these roles.

---

# 8. DNS ARCHITECTURE

## Pi-hole

CT100:

```text
192.168.20.99:53
```

Pi-hole provides local DNS filtering.

Unbound runs behind Pi-hole.

NetBird uses Pi-hole as its DNS resolver.

## Internal service DNS

Internal web-service hostnames generally resolve to CT106 where Nginx is the reverse proxy.

Current split-DNS records include:

```text
panel.robynshomelab.dev    → 192.168.20.94
jellyfin.robynshomelab.dev  → 192.168.20.94
status.robynshomelab.dev    → 192.168.20.94
beszel.robynshomelab.dev    → 192.168.20.94
pihole.robynshomelab.dev    → 192.168.20.94
wings.robynshomelab.dev     → 192.168.20.111
```

The Pterodactyl Panel is therefore reached through Nginx rather than directly through VM108.

---

# 9. CLOUDFLARE

Cloudflare is authoritative for:

```text
robynshomelab.dev
```

Cloudflare is used for:

* Authoritative public DNS.
* DNS-01 ACME validation.
* Public hostname records.
* DDNS.

Cloudflare is **not currently being used as a traffic proxy**.

## Public DNS

Public records intentionally point to the WAN IP where required.

For example:

```text
panel.robynshomelab.dev
    A
    WAN public IP
    DNS only
```

The public Panel record does **not** mean the Panel is publicly reachable.

There is no router port forwarding.

The public Panel record is retained for:

* public DNS consistency
* certificate/DNS-01 infrastructure
* hostname identity

Do not delete or replace the public Panel A record merely because internal clients use split DNS.

## Split DNS

Public:

```text
Cloudflare

panel.robynshomelab.dev
        ↓
WAN IP
```

Internal:

```text
Pi-hole

panel.robynshomelab.dev
        ↓
192.168.20.94
```

This split-DNS arrangement is intentional.

---

# 10. NGINX / CERTBOT

## CT106

Hostname:

```text
nginx
```

IP:

```text
192.168.20.94
```

Services:

* Nginx
* Certbot
* Reverse proxy
* TLS termination

CT106 is the canonical reverse-proxy host.

Current reverse-proxy targets include:

```text
jellyfin.robynshomelab.dev
    → 192.168.20.98:8096

status.robynshomelab.dev
    → 192.168.20.95:3001

beszel.robynshomelab.dev
    → 192.168.20.96:8090

pihole.robynshomelab.dev
    → 192.168.20.99:8080

panel.robynshomelab.dev
    → 192.168.20.111:80
```

Certificates are managed through Certbot using Cloudflare DNS-01.

Current known certificate expiry dates:

```text
jellyfin  → 2026-12-04
status    → 2026-12-04
beszel    → 2026-12-04
pihole    → 2026-12-04
panel     → 2026-12-08
```

Certbot's systemd timer is enabled.

A Certbot DNS-01 dry-run has succeeded.

## Historical architecture

Older documentation may describe Nginx/Certbot as running on CT100 (`192.168.20.99`).

That architecture is **HISTORICAL**.

Current architecture is CT106 (`192.168.20.94`).

---

# 11. PTERODACTYL VM

## VM108

Host:

```text
pve-2
```

VM:

```text
108
```

Hostname:

```text
pterodactyl
```

IP:

```text
192.168.20.111
```

OS:

```text
Debian 13 (Trixie)
```

Resources:

```text
6 vCPU
12 GB RAM
40 GB virtual disk
```

Configuration:

* Q35
* OVMF / UEFI
* VirtIO network
* VirtIO SCSI
* QEMU Guest Agent enabled
* CPU type `host`
* NUMA disabled
* Ballooning disabled
* Nested virtualization disabled
* VM firewall currently disabled

SSH:

```bash
ssh robyn@192.168.20.111
```

NetBird SSH:

```bash
ssh robyn@100.113.229.169
```

---

# 12. PTERODACTYL PANEL

Panel status:

```text
DEPLOYED / VERIFIED
```

Installed components:

* Pterodactyl Panel
* PHP 8.3
* PHP-FPM
* MariaDB 11.8.x
* Redis
* Composer 2.10.x
* Docker Engine
* Nginx

Panel location:

```text
/var/www/pterodactyl
```

Database:

```text
MariaDB
database = panel
```

Credentials are intentionally not stored here.

Laravel configuration includes:

```text
APP_ENV=production
APP_DEBUG=false
APP_TIMEZONE=Australia/Melbourne
APP_URL=https://panel.robynshomelab.dev
```

Panel database migrations and seeding have been completed.

Administrator account has been created.

Laravel storage link has been created.

The VM's local Nginx serves the Panel over HTTP.

Local HTTP access has been verified.

---

# 13. PTERODACTYL PANEL NETWORK PATH

Current working architecture:

```text
LAN / NetBird client
        │
        │ HTTPS
        ▼
CT106 Nginx
192.168.20.94
        │
        │ HTTP :80
        ▼
VM108
192.168.20.111
        │
        ▼
Pterodactyl Panel
```

TLS terminates on CT106.

VM108 does not need to expose Panel HTTPS.

The Panel remains private to LAN + NetBird.

---

# 14. PTERODACTYL HTTPS

Hostname:

```text
panel.robynshomelab.dev
```

Current architecture:

```text
Client
  ↓
HTTPS :443
  ↓
CT106 Nginx
  ↓
HTTP :80
  ↓
VM108 Pterodactyl
```

Certificate acquisition:

```text
CT106 Certbot
      │
      │ Cloudflare API
      ▼
Cloudflare DNS
      │
      │ _acme-challenge TXT
      ▼
Let's Encrypt
```

Use **DNS-01**, not HTTP-01.

No router port forwarding is required.

The dedicated Cloudflare ACME credential is separate from the DDNS credential.

Never document the actual credentials.

Panel HTTPS is:

```text
DEPLOYED / VERIFIED
```

---

# 15. PTERODACTYL WINGS

Wings is now:

```text
DEPLOYED / VERIFIED
```

Installed binary:

```text
/usr/local/bin/wings
```

Current version:

```text
1.13.3
```

Configuration:

```text
/etc/pterodactyl/config.yml
```

Configuration permissions are restricted.

Systemd service:

```text
/etc/systemd/system/wings.service
```

The service is enabled and running.

Wings uses Docker for game-server containers.

Current daemon configuration includes:

```text
Daemon directory:
/var/lib/pterodactyl/volumes

Daemon port:
8080

SFTP:
2022
```

The Pterodactyl node is configured as:

```text
Name:
pterodactyl

Location:
Home

FQDN:
wings.robynshomelab.dev

SSL:
enabled

Behind proxy:
no

Memory:
10240 MiB

Disk:
30720 MiB
```

Wings storage and NAS storage are intentionally separate.

---

# 16. NETBIRD

## CT101 routing peer

NetBird routing peer:

```text
CT101
192.168.20.97
```

NetBird version:

```text
0.78.1
```

NetBird virtual address:

```text
100.113.51.59/16
```

Service:

```text
netbird.service
```

Systemd service is enabled and running.

Current routed LAN addresses:

```text
192.168.20.94/32
192.168.20.99/32
```

These provide NetBird access to:

* CT106 Nginx
* CT100 Pi-hole

## NetBird DNS

NetBird global/default DNS uses:

```text
192.168.20.99:53
```

for:

```text
[.]
```

DNS traffic from NetBird clients to Pi-hole has previously been verified with packet capture.

## Panel routing

NetBird clients do not need a direct route to VM108 for Panel access.

The intended path is:

```text
NetBird client
    ↓
CT101
    ↓
192.168.20.94/32
    ↓
CT106
    ↓
192.168.20.111
    ↓
Pterodactyl
```

---

# 17. VM108 NETBIRD

VM108 now has its own independent NetBird peer.

This is separate from CT101.

Current VM108 NetBird information:

```text
NetBird version:
0.78.1

NetBird FQDN:
pterodactyl.netbird.cloud

NetBird IP:
100.113.229.169/16
```

CT101 and VM108 must remain independent NetBird peers.

CT101 is the LAN routing peer.

VM108's NetBird identity is primarily used for:

* SSH
* SFTP
* private management
* Pterodactyl/Wings networking
* game-server networking
* future NetBird-based game-server exposure

Do not route VM108's traffic through CT101 merely because both are NetBird peers.

---

# 18. GAME SERVER NETWORKING

Game-server public/remote networking is **IN PROGRESS / NOT FULLY FINALIZED**.

The desired architecture is:

```text
Internet
    │
    ▼
NetBird Reverse Proxy
    │
    ▼
NetBird tunnel
    │
    ▼
VM108
    │
    ▼
Docker / Wings
    │
    ▼
Specific game server
```

The Pterodactyl Panel remains private.

Do not expose the Panel through the public game-server proxy.

Do not directly port-forward game-server ports through the home router unless the architecture is explicitly changed.

Only required TCP/UDP game allocations should eventually be exposed.

A previous NetBird managed Reverse Proxy experiment successfully established a public TCP connection, but traffic did not reach VM108 as expected. That investigation was deliberately paused.

Do not resume that troubleshooting unless requested.

---

# 19. FIREWALL

Final VM108 firewall hardening has **not yet been applied**.

Planned technology:

```text
nftables
```

Do not blindly apply UFW.

Desired eventual policy:

```text
Default deny incoming

Allow:
- established/related
- loopback
- SSH 22 from LAN + NetBird
- required management traffic
- Wings API from appropriate private sources
- SFTP 2022 from LAN + NetBird
- only allocated game-server TCP/UDP ports
```

Docker creates its own networking/firewall rules.

Therefore the final nftables design must account for Docker and Wings before being applied.

---

# 20. STORAGE / NAS

## Physical storage

The Dell OptiPlex 9020 is now the active storage host.

Drive:

```text
WD Blue
WDC WD40EZRZ-00GXCB
```

Capacity:

```text
4,000,787,030,016 bytes
```

Approximately:

```text
4 TB raw
~3.6T Linux usable capacity before filesystem overhead
```

Drive characteristics observed:

* 5400 RPM
* CMR
* SATA 3.1 / 6 Gb/s
* 512-byte logical sectors
* 4096-byte physical sectors

SMART has passed.

Observed SMART state showed:

* no reallocated sectors
* no pending sectors
* no uncorrectable sectors
* no CRC errors
* approximately 9000 power-on hours
* approximately 20°C at time of inspection

The extended SMART test was not run because the estimated runtime was several hours.

## Filesystem

The disk was wiped and repartitioned.

Filesystem:

```text
ext4
```

Label:

```text
homelab-data
```

UUID:

```text
5ea1368a-7c7b-4fec-871d-537b8a8bf62b
```

Persistent mount:

```text
/mnt/homelab-data
```

Directory structure:

```text
/mnt/homelab-data/
├── media/
│   ├── anime/
│   ├── books/
│   ├── movies/
│   ├── music/
│   └── tv/
├── downloads/
│   ├── incomplete/
│   └── complete/
├── games/
├── backups/
└── shared/
```

---

# 21. NFS STORAGE

NFS is now:

```text
DEPLOYED / VERIFIED
```

NFS server:

```text
pve-2
192.168.20.101
```

Package:

```text
nfs-kernel-server
```

## Games export

Pterodactyl game storage is exported separately:

```text
/mnt/homelab-data/games
```

to:

```text
192.168.20.111/32
```

with root squashing.

VM108 successfully mounted the share using NFSv4.2 and successfully wrote a test file.

## Media/download exports

Media and downloads are exported specifically to the required hosts.

Current effective export design:

```text
/mnt/homelab-data/media
    → pve-1
    → RobynPC

/mnt/homelab-data/downloads
    → pve-1
```

Media/download exports use:

```text
all_squash
anonuid=999
anongid=990
```

This ensures NFS-created media/download files map to the intended `mediastack` ownership.

The `mediastack` identity is:

```text
UID 999
GID 990
```

on pve-2.

The media and downloads trees are owned accordingly.

---

# 22. PVE-1 STORAGE MOUNTS

pve-1 mounts the NFS media/download storage from pve-2.

Media mount:

```text
/mnt/homelab-media
```

Downloads mount:

```text
/mnt/homelab-downloads
```

The mounts are persistent and use NFS4/systemd automount behaviour.

CT103 receives these host mounts through LXC bind mounts.

---

# 23. CT103 MEDIA STACK

CT103:

```text
Name:
mediastack

IP:
192.168.20.93

OS:
Debian 12 Bookworm

CPU:
4 cores

RAM:
4096 MB

Type:
unprivileged LXC
```

Docker is installed and operational.

Current Docker stack includes:

```text
qBittorrent
Prowlarr
Sonarr
Radarr
Bazarr
Seerr
FlareSolverr
```

Docker configuration uses:

```text
/opt/media-stack/config/
```

for persistent service configuration.

---

# 24. MEDIA STORAGE PATHS

Inside CT103:

```text
/mnt/media
/mnt/downloads
```

These are backed by NFS storage on pve-2 through pve-1.

Media structure:

```text
/mnt/media/
├── anime/
├── books/
├── movies/
├── music/
└── tv/
```

Downloads:

```text
/mnt/downloads/
├── incomplete/
└── complete/
```

Docker containers use the same canonical paths where required.

This is intentional so Radarr/Sonarr/qBittorrent can correctly reference the same filesystem paths.

---

# 25. QBITTORRENT

qBittorrent is deployed through:

```text
lscr.io/linuxserver/qbittorrent
```

Container mapping includes:

```text
/mnt/downloads → /mnt/downloads
```

Web UI:

```text
8080
```

PUID/PGID:

```text
1000:1000
```

A write test through Docker was successful.

A manually added torrent was successfully downloaded to:

```text
/mnt/downloads/complete/
```

The test file:

```text
Ghost.in.the.Shell.1995.1080p.BluRay.Opus2.0-AV1-Tasokare.mkv
```

was subsequently moved into:

```text
/mnt/media/anime/
```

Do not treat this manually moved file as an indication that Radarr/Sonarr imported it.

---

# 26. RADARR / SONARR

Radarr:

```text
Port:
7878

Container hostname:
radarr

Movie root:
 /mnt/media/movies
```

Sonarr:

```text
Port:
8989

Container hostname:
sonarr

TV root:
 /mnt/media/tv

Anime root:
 /mnt/media/anime
```

Both are connected to Seerr.

Both have automatic searching enabled.

Both are configured to scan completed imports.

---

# 27. PROWLARR / FLARESOLVERR

Prowlarr:

```text
Port:
9696
```

Five indexers were configured.

Three are currently relevant to the Radarr movie workflow because the other two are not movie-capable.

FlareSolverr:

```text
Port:
8191
```

Prowlarr's FlareSolverr proxy is configured as:

```text
Host:
http://flaresolverr:8191

Tag:
flaresolverr
```

Only indexers that require the proxy should use the tag.

The configured indexers were tested and are currently healthy.

Do not recommend piracy trackers or additional evasion infrastructure.

---

# 28. SEERR

Seerr:

```text
Port:
5055
```

Version:

```text
3.4.1
```

Initial configuration issue involving permissions on:

```text
/opt/media-stack/config/seerr/logs/
```

was resolved by assigning ownership to the container's `node` user:

```text
UID 1000
GID 1000
```

Seerr is now:

```text
DEPLOYED / VERIFIED
```

Seerr is connected to:

* Jellyfin
* Radarr
* Sonarr

Jellyfin connection:

```text
192.168.20.98:8096
```

Radarr:

```text
radarr:7878
/mnt/media/movies
```

Sonarr:

```text
sonarr:8989
/mnt/media/tv
/mnt/media/anime
```

An end-to-end movie request was successfully tested.

Test workflow:

```text
Seerr
  ↓
Radarr
  ↓
Prowlarr
  ↓
qBittorrent
  ↓
NFS downloads
  ↓
Radarr import
  ↓
NFS media
  ↓
Jellyfin
```

Test movie:

```text
Disclosure Day (2026)
```

Successfully imported to:

```text
/mnt/media/movies/Disclosure Day (2026)/
```

---

# 29. JELLYFIN

CT102:

```text
192.168.20.98
```

Jellyfin version:

```text
10.11.11.0
```

Installed packages include:

```text
jellyfin
jellyfin-server
jellyfin-web
jellyfin-ffmpeg7
```

Jellyfin uses the NFS-backed media storage.

A stale NFS handle occurred after NFS export changes.

The issue was resolved by:

1. stopping CT102
2. remounting the pve-1 NFS media mount
3. restarting CT102
4. rescanning the Jellyfin library

The library scan then succeeded.

Do not perform unnecessary per-file refreshes.

If stale NFS handles recur, first inspect the actual mount/export state.

---

# 30. JELLYFIN AUTOMATIC REFRESH

Automatic Jellyfin library refresh after Radarr/Sonarr imports is **NOT YET CONFIGURED**.

Planned mechanism:

* Create a Jellyfin API key.
* Use a small refresh script on CT103.
* Expose the script to Radarr/Sonarr as a Custom Script.
* Trigger a Jellyfin library refresh after successful imports.

Proposed endpoint:

```text
http://192.168.20.98:8096/Library/Refresh
```

The API key must never be stored in this AI handover or GitHub.

---

# 31. MOONBASE / MOONFIN

Moonfin is now:

```text
DEPLOYED / VERIFIED
```

The Jellyfin server uses the Moonbase plugin to provide Moonfin server-side functionality.

Jellyfin:

```text
10.11.11.0
```

Moonbase was manually installed because it was not exposed through the normal Jellyfin plugin catalog.

Installed plugin directory:

```text
/var/lib/jellyfin/plugins/Moonbase
```

The installed plugin contains the Moonfin server plugin and supporting frontend files.

Jellyfin restarted successfully after installation.

## Moonbase configuration

Moonbase Sync:

```text
ON
```

Seerr integration:

```text
ON
```

Seerr URL:

```text
http://192.168.20.93:5055
```

Public Server URL configured for the integration:

```text
http://192.168.20.98:8096
```

Moonbase provides the server-side Seerr proxy/SSO functionality required by Moonfin.

Moonfin Android TV is currently **working**.

The earlier Jellystack Android TV sidebar/focus problem is considered historical and is not the current client architecture.

## Moonbase webhook status

Moonbase previously reported:

```text
NoAdminSession.
No admin Seerr session available yet.
Configure the webhook manually or sign in as a Seerr admin.
```

This did not prevent the core Moonfin/Seerr integration from working.

Do not alter the webhook configuration unnecessarily unless a specific Moonbase/Seerr feature requires it.

---

# 32. MONITORING

Currently deployed:

```text
CT104 Beszel
CT105 Uptime Kuma
```

Pterodactyl/VM108 monitoring is not yet fully configured.

Future monitoring should include, where useful:

* VM108 health
* Pterodactyl Panel
* Wings
* game servers
* storage
* Docker
* NFS
* network connectivity
* media stack

Do not document monitors as deployed until they actually exist.

---

# 33. HOMARR

VMID:

```text
107
```

Status:

```text
RESERVED / PLANNED
```

Target host:

```text
pve-1
```

Desired role:

* Homelab dashboard
* Service links
* Infrastructure overview
* Monitoring integration
* Resource information
* Service health

Desired visual style:

* dark
* glass/translucent
* compact
* polished
* strong icon usage
* monitoring-focused

Do not treat Homarr as deployed until installation is confirmed.

---

# 34. DDNS

DDNS runs from CT100.

CT100:

```text
192.168.20.99
```

Cloudflare DDNS uses a dedicated credential.

Do not reuse or document the actual credential.

DDNS and ACME credentials must remain separate.

---

# 35. DESKTOP

Desktop:

```text
Hostname:
RobynPC

OS:
Arch Linux

Interface:
enp5s0

MAC:
d4:5d:64:d7:d7:59

Current IP:
192.168.20.102
```

The router DHCP pool was changed to begin at `.150`.

The desktop has a DHCP reservation for:

```text
192.168.20.102
```

based on its MAC address.

RobynPC mounts the NFS media share from pve-2.

Current media mount:

```text
/mnt/homelab-media
```

---

# 36. CURRENT STORAGE ARCHITECTURE

```text
                     pve-2
               192.168.20.101
                      │
          ┌───────────┴───────────┐
          │                       │
       SSD                     4 TB HDD
          │                       │
     Proxmox VE            /mnt/homelab-data
                                  │
                 ┌────────────────┼────────────────┐
                 │                │                │
               media          downloads          games
                 │                │                │
                 │                │                │
              NFS              NFS              NFS
                 │                │                │
                 ▼                ▼                ▼
              pve-1            pve-1            VM108
                 │                │
                 ▼                ▼
              CT103            CT103
           Media stack      Media stack
```

Important:

* Pterodactyl game storage is logically separate from media/download storage.
* The HDD is currently a simple host-mounted ext4 storage solution.
* TrueNAS and OpenMediaVault are not part of the current architecture.
* Do not migrate this to another NAS operating system without an explicit decision.

---

# 37. CURRENT MEDIA ARCHITECTURE

```text
                    Seerr
                      │
          ┌───────────┴───────────┐
          ▼                       ▼
       Radarr                  Sonarr
          │                       │
          └───────────┬───────────┘
                      ▼
                  Prowlarr
                      │
                      ▼
                qBittorrent
                      │
                      ▼
              /mnt/downloads
                      │
                      ▼
              import/move
                      │
                      ▼
                /mnt/media
                      │
                      ▼
                  Jellyfin
```

All media/download filesystem paths are backed by the pve-2 HDD through NFS.

---

# 38. CURRENT PTERODACTYL ARCHITECTURE

```text
                         Client
                           │
                    HTTPS / NetBird
                           │
                           ▼
                    CT106 Nginx
                   192.168.20.94
                           │
                       HTTP :80
                           │
                           ▼
                   VM108 Pterodactyl
                   192.168.20.111
                           │
              ┌────────────┴────────────┐
              │                         │
           Panel                     Wings
              │                         │
              │                       Docker
              │                         │
              │                  Game containers
              │
              └───────────────┐
                              │
                           Control
                              │
                              ▼
                         Game servers
```

Panel:

```text
Private LAN + NetBird
```

Game-server networking:

```text
Still being finalized
```

---

# 39. CURRENTLY COMPLETED WORK

## Proxmox

* pve-1 deployed.
* pve-2 deployed.
* pve-2 joined the `homelab` cluster.
* Both Proxmox nodes are members of the same cluster.
* HA intentionally disabled.

## Networking

* LAN architecture established.
* NetBird routing peer operational.
* NetBird routes `.94/32` and `.99/32`.
* NetBird DNS routes through Pi-hole.
* DNS traffic verified with packet capture.
* VM108 has an independent NetBird peer.
* No router port forwarding required for private services.

## Pterodactyl

* VM108 created on pve-2.
* Debian 13 installed.
* Docker installed.
* PHP 8.3 installed.
* MariaDB installed.
* Redis installed.
* Composer installed.
* Pterodactyl Panel installed.
* Database created and migrated.
* Administrator account created.
* Laravel storage link created.
* Local Nginx configured.
* Panel HTTP access verified.
* Wings installed.
* Wings configured.
* Wings systemd service enabled/running.
* Pterodactyl node configured.
* Panel successfully reverse-proxied through CT106.
* Panel HTTPS working.

## Reverse proxy

* CT106 exists.
* Nginx deployed.
* Certbot deployed.
* Pterodactyl reverse proxy working.
* TLS terminates at CT106.
* Cloudflare DNS-01 working.
* Certificates successfully issued.

## Storage

* 4 TB WD40EZRZ installed.
* HDD wiped.
* GPT partition created.
* ext4 filesystem created.
* Persistent pve-2 mount configured.
* Storage directory structure created.
* NFS server installed.
* Media NFS export working.
* Downloads NFS export working.
* Games NFS export working.
* VM108 successfully mounted games NFS.
* pve-1 successfully mounted media/downloads.
* CT103 successfully uses NFS-backed media/downloads.
* Desktop successfully mounts media NFS.

## Media stack

* CT103 deployed.
* Docker deployed.
* qBittorrent deployed.
* Prowlarr deployed.
* Sonarr deployed.
* Radarr deployed.
* Bazarr deployed.
* Seerr deployed.
* FlareSolverr deployed.
* Prowlarr/FlareSolverr integration working.
* Seerr connected to Jellyfin.
* Seerr connected to Radarr/Sonarr.
* End-to-end movie request successfully tested.
* Radarr successfully imported the test movie.
* Jellyfin successfully scanned the imported media.

## Jellyfin / Moonfin

* Moonbase manually installed.
* Moonbase Sync enabled.
* Seerr integration configured.
* Moonfin Android TV configured.
* Moonfin currently working.

---

# 40. CURRENT IN-PROGRESS WORK

The following areas are active but not completely finalized:

### Pterodactyl game networking

* NetBird-based public game-server architecture.
* Reverse proxy/game-server exposure.
* Actual game allocations.
* First production game server.

### VM108 firewall

* Final nftables rules.
* Docker/Wings interaction.
* Game allocation-specific rules.

### Monitoring

* Full VM108/Wings/Pterodactyl monitoring.
* Storage monitoring.
* Media stack monitoring.

### Jellyfin automation

* Automatic library refresh following Radarr/Sonarr imports.

---

# 41. PLANNED WORK

## Homarr

VMID 107 remains reserved.

## Game servers

* Deploy first game server.
* Determine required allocations.
* Test actual gameplay connectivity.
* Finalize NetBird networking.
* Add monitoring.

## Network improvements

Potential future:

* Managed switch.
* VLAN segmentation.
* Potential pfSense/router replacement.

These are future architecture decisions and must not be assumed to exist.

---

# 42. HISTORICAL / OBSOLETE ARCHITECTURES

The following should not be treated as current:

### Nginx on CT100

Historical:

```text
CT100
192.168.20.99
```

Current:

```text
CT106
192.168.20.94
```

### Media stack as VMID 103 reserved

Historical.

Current:

```text
CT103
192.168.20.93
```

### Wings not installed

Historical.

Current:

```text
Wings 1.13.3
DEPLOYED / VERIFIED
```

### VM108 without NetBird

Historical.

Current:

```text
VM108 has an independent NetBird peer.
```

### HDD not installed

Historical.

Current:

```text
4 TB WD40EZRZ installed and serving NFS storage.
```

### Media stack planned

Historical.

Current:

```text
Media stack deployed and end-to-end tested.
```

### Moonfin not installed

Historical.

Current:

```text
Moonbase + Moonfin deployed and working.
```

---

# 43. IMMEDIATE NEXT STEPS

Proceed one step at a time and verify each step before continuing.

## Step 1 — Jellyfin automatic refresh

Complete the Radarr/Sonarr → Jellyfin automatic refresh mechanism.

Do not expose or document the Jellyfin API key.

## Step 2 — Pterodactyl game server

Create the first actual game server and allocation.

Use this to determine real TCP/UDP networking requirements rather than designing firewall rules from assumptions.

## Step 3 — Test game networking

Test the actual game server through the intended NetBird architecture.

Do not resume the earlier paused reverse-proxy troubleshooting unless the actual game-server test requires it.

## Step 4 — VM108 firewall

Once actual allocations are known:

* design nftables rules
* account for Docker
* account for Wings
* default deny incoming
* allow only required services

## Step 5 — Monitoring

Add:

* VM108
* Wings
* Pterodactyl
* game server
* relevant storage/NFS health

to Beszel/Uptime Kuma as appropriate.

## Step 6 — Homarr

Deploy VMID 107 when desired.

---

# 44. DOCUMENTATION RULES

Future documentation must follow these rules:

1. `192.168.20.93` = CT103 Media Stack.
2. `192.168.20.94` = CT106 Nginx/Certbot.
3. `192.168.20.97` = CT101 NetBird routing peer.
4. `192.168.20.99` = CT100 Pi-hole/Unbound/ddclient.
5. `192.168.20.111` = VM108 Pterodactyl.
6. pve-1 and pve-2 are already members of Proxmox cluster `homelab`.
7. pve-2 is deployed and operational.
8. VM108 is deployed.
9. Pterodactyl Panel is deployed.
10. Pterodactyl Wings is deployed.
11. VM108 has an independent NetBird peer.
12. The 4 TB WD40EZRZ is installed and operational.
13. NFS is deployed and operational.
14. CT103 is deployed and operational.
15. The media stack is deployed and has passed an end-to-end test.
16. Moonbase/Moonfin is deployed and working.
17. Homarr is not deployed.
18. Panel access is private LAN + NetBird.
19. No router port forwarding is currently used.
20. Cloudflare is authoritative DNS and DNS-01 infrastructure, not the traffic proxy.
21. Public Panel DNS points to the WAN IP.
22. Internal Panel DNS points to CT106 `.94`.
23. TLS terminates on CT106.
24. VM108 serves the Panel over HTTP internally.
25. Game-server networking is not yet finalized.
26. Final VM108 firewall hardening is not yet complete.
27. Never store secrets in GitHub documentation.
28. Do not create duplicate numbered human documentation when an existing canonical document already covers the topic.
29. Existing human documentation numbering/nomenclature must be preserved unless explicitly changed by the user.
30. When documentation and deployed state disagree, deployed state wins.

---

# 45. TROUBLESHOOTING PRINCIPLES

When continuing work:

* Check actual service state before changing configuration.
* Prefer native service status commands.
* Use logs when diagnosing failures.
* Use packet capture when diagnosing DNS/network behaviour.
* Distinguish DNS filtering from DNS failure.
* Distinguish NetBird peer registration from peer connectivity.
* Do not interpret historical logs as current failures without checking current status.
* Do not rebuild known-good infrastructure because documentation is stale.
* Test each network layer independently.
* Preserve working configuration before making changes.
* When possible, make one change at a time and verify it.
* For NFS issues, check the server export, client mount, and actual filesystem path independently.
* Do not repeatedly force Jellyfin library refreshes unless there is evidence of an indexing/mount problem.
* For Docker problems, inspect container state and logs before changing permissions or rebuilding containers.
* For Pterodactyl problems, distinguish Panel, Wings, Docker, game allocation, and network-layer failures.
* Do not assume a successful TCP connection to a NetBird reverse proxy means the connection is reaching the intended backend.

---

# 46. SECURITY

Never store:

* passwords
* API tokens
* Cloudflare credentials
* NetBird setup keys
* private keys
* session cookies
* authentication tokens
* SSH private keys
* database passwords
* Jellyfin API keys
* Pterodactyl credentials

If a credential is needed, obtain/configure it directly on the target system.

Public documentation should contain architecture and configuration concepts, not secrets.

---

# 47. CURRENT CANONICAL STATE MAP

```text
ROBYN'S HOMELAB

domain = robynshomelab.dev

LAN = 192.168.20.0/24

router = 192.168.20.1

==================================================
PROXMOX CLUSTER
==================================================

name = homelab

pve-1 = 192.168.20.100
pve-2 = 192.168.20.101

nodes = 2
HA = disabled

==================================================
SERVICES
==================================================

CT100
  name = pihole
  IP = 192.168.20.99
  Pi-hole
  Unbound
  ddclient
  status = DEPLOYED / VERIFIED

CT101
  name = netbird
  IP = 192.168.20.97
  NetBird routing peer
  routes = 192.168.20.94/32, 192.168.20.99/32
  DNS = 192.168.20.99:53
  status = DEPLOYED / VERIFIED

CT102
  name = jellyfin
  IP = 192.168.20.98
  Jellyfin 10.11.11
  Moonbase
  Moonfin integration
  status = DEPLOYED / VERIFIED

CT103
  name = mediastack
  IP = 192.168.20.93
  Docker
  qBittorrent
  Prowlarr
  Sonarr
  Radarr
  Bazarr
  Seerr
  FlareSolverr
  status = DEPLOYED / VERIFIED

CT104
  name = beszel
  IP = 192.168.20.96
  status = DEPLOYED / VERIFIED

CT105
  name = uptime-kuma
  IP = 192.168.20.95
  status = DEPLOYED / VERIFIED

CT106
  name = nginx
  IP = 192.168.20.94
  Nginx
  Certbot
  reverse proxy
  TLS termination
  status = DEPLOYED / VERIFIED

VMID 107
  Homarr
  status = RESERVED / PLANNED

VM108
  name = pterodactyl
  host = pve-2
  IP = 192.168.20.111
  Debian 13
  6 vCPU
  12 GB RAM
  40 GB disk
  Panel = DEPLOYED / VERIFIED
  Wings 1.13.3 = DEPLOYED / VERIFIED
  NetBird = DEPLOYED / VERIFIED

==================================================
PTERODACTYL
==================================================

public hostname =
  panel.robynshomelab.dev

internal DNS =
  192.168.20.94

reverse proxy =
  CT106

backend =
  192.168.20.111:80

TLS =
  CT106

access =
  LAN + NetBird

router port forwarding =
  NONE

Wings =
  VM108

Wings daemon =
  :8080

SFTP =
  :2022

==================================================
CLOUDFLARE
==================================================

authoritative DNS =
  YES

traffic proxy =
  NO

Panel public A record =
  WAN IP

ACME =
  DNS-01

DDNS =
  CT100

==================================================
NETBIRD
==================================================

routing peer =
  CT101

CT101 NetBird IP =
  100.113.51.59

LAN routes =
  .94/32
  .99/32

DNS =
  .99:53

VM108 NetBird =
  independent peer

VM108 NetBird IP =
  100.113.229.169

==================================================
STORAGE
==================================================

storage host =
  pve-2

disk =
  WD40EZRZ 4 TB

filesystem =
  ext4

mount =
  /mnt/homelab-data

NFS =
  DEPLOYED / VERIFIED

exports =
  media
  downloads
  games

==================================================
MEDIA
==================================================

host =
  CT103

IP =
  192.168.20.93

request =
  Seerr

automation =
  Radarr / Sonarr

indexers =
  Prowlarr

download =
  qBittorrent

proxy helper =
  FlareSolverr

subtitles =
  Bazarr

library =
  Jellyfin

storage =
  NFS → pve-2

end-to-end test =
  PASSED

==================================================
MOONFIN
==================================================

Jellyfin plugin =
  Moonbase

Moonbase Sync =
  ON

Seerr integration =
  ON

Moonfin Android TV =
  WORKING

==================================================
STATUS
==================================================

core infrastructure =
  DEPLOYED / VERIFIED

Pterodactyl Panel =
  DEPLOYED / VERIFIED

Pterodactyl HTTPS =
  DEPLOYED / VERIFIED

Wings =
  DEPLOYED / VERIFIED

VM108 NetBird =
  DEPLOYED / VERIFIED

NAS/NFS =
  DEPLOYED / VERIFIED

media stack =
  DEPLOYED / VERIFIED

Moonfin =
  DEPLOYED / VERIFIED

Jellyfin automatic refresh =
  PLANNED

game servers =
  PLANNED / IN PROGRESS

game-server networking =
  IN PROGRESS

final VM108 firewall =
  PLANNED

monitoring expansion =
  PLANNED

Homarr =
  RESERVED / PLANNED
```
# AI Handover Update — September 2026

## Major completed work

### Pterodactyl Minecraft public networking — DEPLOYED / VERIFIED

VM108 now has permanent public Minecraft connectivity through NetBird Reverse Proxy.

Pterodactyl VM:

* LAN: `192.168.20.111`
* NetBird: `100.113.229.169`
* NetBird hostname: `pterodactyl.netbird.cloud`

Minecraft:

* Version: `1.21.1`
* Protocol: `767`
* Server port: `25565`

Permanent public endpoint:

```text
minecraft.robynshomelab.dev:17161
```

NetBird Reverse Proxy target:

```text
100.113.229.169:25565
```

Cloudflare is authoritative for `robynshomelab.dev`.

NetBird custom-domain verification is complete for:

```text
minecraft.robynshomelab.dev
```

Required wildcard DNS verification record:

```text
*.minecraft.robynshomelab.dev
CNAME → eu1.netbird.services
```

Cloudflare DNS is DNS-only.

The old temporary Minecraft CNAME/SRV records have been removed.

The NetBird Reverse Proxy service list has been cleaned up. The permanent Minecraft service is the only Minecraft reverse-proxy service remaining.

### Minecraft networking problem and resolution

The original problem was intermittent connectivity through NetBird's temporary CLI exposure:

```bash
netbird expose --protocol tcp 25565
```

Temporary endpoints could successfully connect to Minecraft but could also immediately return `connection refused` on subsequent attempts.

Testing established:

* Minecraft itself was healthy.
* `192.168.20.111:25565` was reachable locally.
* Minecraft status handshakes succeeded.
* Traffic was reaching the VM through NetBird's `wt0`.
* The DNAT rule was receiving packets even during failed connection attempts.
* Temporary NetBird endpoints resolved through multiple public proxy addresses and displayed intermittent connection behaviour.

The exact internal reason for the temporary proxy instability was not conclusively established and should not be described as confirmed rate limiting, DDoS protection, or another specific NetBird mechanism.

The solution was to stop relying on ephemeral `netbird expose` endpoints and use a permanent NetBird Reverse Proxy service with the verified custom domain:

```text
minecraft.robynshomelab.dev:17161
```

The permanent endpoint successfully completed a Minecraft protocol handshake and was subsequently confirmed from the actual Minecraft client.

A `tcpdump` capture on `wt0` also confirmed that connections using `minecraft.robynshomelab.dev` were reaching the Pterodactyl VM.

The existing DNAT rule remains required:

```bash
/usr/sbin/iptables -t nat -I PREROUTING 1 \
  -i wt0 \
  -p tcp \
  -d 100.113.229.169 \
  --dport 25565 \
  -j DNAT \
  --to-destination 192.168.20.111:25565
```

It is persisted using `netfilter-persistent`.

### Temporary NetBird endpoints

Temporary endpoints used during troubleshooting:

```text
upokwfkzqzjy.eu1.netbird.services:29387
kkvy1yzwqnxv.eu1.netbird.services:46767
```

These are not part of the final configuration.

They were created with `netbird expose` and should not be recreated unless specifically needed for diagnostics.

### Current Pterodactyl status

Pterodactyl VM108 remains operational with:

* Pterodactyl Panel
* Wings
* Docker
* MariaDB
* Redis
* NetBird

Panel:

```text
https://panel.robynshomelab.dev
```

Panel traffic continues through CT106 Nginx at `192.168.20.94`.

Minecraft public traffic uses NetBird Reverse Proxy rather than the Pterodactyl Panel reverse proxy.

Do not move Minecraft behind CT106 Nginx; the existing NetBird Layer-4 TCP architecture is intentional.

---

## Current homelab networking notes

NetBird version remains:

```text
0.78.1
```

VM108 is an independent NetBird peer from CT101.

VM108:

```text
100.113.229.169/16
pterodactyl.netbird.cloud
```

CT101:

```text
192.168.20.97
100.113.51.59/16
```

Do not treat CT101 as the gateway for VM108.

The home router does not need a Minecraft port-forwarding rule.

The user explicitly prefers avoiding router port forwarding where NetBird can provide the required connectivity.

---

## User workflow preferences

The user has explicitly requested that troubleshooting be efficient and avoid unnecessary rabbit holes.

When a script is required:

* Provide a complete fresh script.
* Make it directly pasteable into the terminal whenever practical.
* Do not ask the user to download a script and edit it manually.
* Do not ask the user to modify an existing script when a complete replacement can be provided.

When troubleshooting:

* Use information already documented in the handover/current conversation.
* Do not repeatedly request information that is already known.
* Prefer targeted diagnostics over repeated generic connectivity tests.
* Once a configuration has been proven working, avoid changing it unnecessarily.

---

## Changelog

### 2026-09-19

* Completed permanent NetBird Reverse Proxy setup for Minecraft.
* Verified `minecraft.robynshomelab.dev` as a NetBird custom domain.
* Created permanent Minecraft TCP reverse-proxy service.
* Public endpoint assigned: `minecraft.robynshomelab.dev:17161`.
* Backend configured as `100.113.229.169:25565`.
* Confirmed Minecraft protocol handshake through the permanent endpoint.
* Confirmed actual Minecraft client connectivity.
* Captured Minecraft traffic on `wt0` and confirmed `minecraft.robynshomelab.dev` traffic reaches VM108.
* Confirmed existing DNAT rule is functioning and persisted.
* Removed obsolete Cloudflare Minecraft CNAME/SRV records.
* Removed/retired temporary NetBird exposure configuration from the final architecture.
* Cleaned NetBird Reverse Proxy services so only the permanent Minecraft service remains.
* Documented the intermittent temporary-exposure problem and its resolution.
* Confirmed no router port forwarding is required for Minecraft.

---

## END OF AI CONTEXT
